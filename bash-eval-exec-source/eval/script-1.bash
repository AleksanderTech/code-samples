#!/bin/bash

# Dynamic execution of a command
# It takes a string as an argument, treats it as a line of code, and then executes it.
# command="echo 'Hello, World!'"
# eval $command

eval "bash script-2.bash"
echo "end"
